<div class="list-group list-group-flush ">
  {{#each professors}}
  <a href="{{../name}}/{{this.id}}" class="list-group-item p-3 border-0 d-flex list-group-item-action justify-content-between">
    {{this.name}}
    <span class="list-star">
      ({{random}} calificaciones)
      {{{ratingToStars this.rating}}}
    </span>
  </a>
  {{/each}}
</div>
<button class="btn btn-light m-1">Cargar más</button>